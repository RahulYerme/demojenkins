pipeline {
 agent any
 tools {
   maven 'maven'
 }
 stages{
  stage("Version"){
   steps{
    script{
     va_r = "${BRANCH_NAME} -${BUILD_NUMBER}"
     echo "${va_r}"
    }
   }
  }
  stage("build"){
   steps {
    script{
      
    }
    
   }
  }
  stage("Local Archive"){
   steps {
    archive 
      
    }
    
   }
  }
  stage("Deploy the application..."){
   steps{
    echo "Deploying the appliaction.."
   }
  }
 }
}
