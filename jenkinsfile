pipeline {
 agent any 
 stages{
  stage("build"){
   steps {
    echo "Building the java application..."
   }
  }
  stage("Deploy the application..."){
   steps{
    echo "Deploying the appliaction.."
   }
  }
 }
}
